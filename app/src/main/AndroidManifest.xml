<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.bluehouse.enablevolte">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for BootReceiver to receive BOOT_COMPLETED -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required for AutoApplyService (foreground service) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 10+: connected-device foreground service type — allowed from BOOT_COMPLETED
         on all Android versions. Semantically correct: configuring SIM/modem connectivity.
         connectedDevice FGS requires FOREGROUND_SERVICE_CONNECTED_DEVICE + at least one of the
         device-connectivity permissions. CHANGE_NETWORK_STATE is accurate (we change carrier
         network config) and is a normal permission (no runtime prompt needed). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- Android 13+: show notification from AutoApplyService -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <instrumentation
        android:name="dev.bluehouse.enablevolte.BrokerInstrumentation"
        android:targetPackage="dev.bluehouse.enablevolte"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnableVoLTE"
        tools:targetApi="36"
        android:localeConfig="@xml/locales_config">
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.EnableVoLTE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives BOOT_COMPLETED and starts AutoApplyService -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- HTC/Huawei quick-boot support -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Foreground service that waits for Shizuku and re-applies carrier config on boot.
             connectedDevice is allowed to start from BOOT_COMPLETED on Android 10+ (API 29+),
             including Android 15/16 where dataSync and shortService are restricted from boot. -->
        <service
            android:name=".AutoApplyService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name=".SIM1VoLTEConfigToggleQSTileService"
            android:exported="true"
            android:label="@string/qs_toggle_tile_title_sim_1"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <service
            android:name=".SIM2VoLTEConfigToggleQSTileService"
            android:exported="true"
            android:label="@string/qs_toggle_tile_title_sim_2"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <service
            android:name=".SIM1IMSStatusQSTileService"
            android:exported="true"
            android:label="@string/qs_status_tile_title_sim_1"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".SIM2IMSStatusQSTileService"
            android:exported="true"
            android:label="@string/qs_status_tile_title_sim_2"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
